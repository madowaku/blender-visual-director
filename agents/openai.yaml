interface:
  display_name: "Blender Visual Director"
  short_description: "BlenderMCP creation, target matching, selective visual review"
  default_prompt: "Use $blender-visual-director to create or revise this Blender scene with batched MCP edits and only the visual checkpoints needed to judge quality. If a target image or critic loop is present, preserve target/critic ownership and return minimal fresh evidence for the next comparison round."
